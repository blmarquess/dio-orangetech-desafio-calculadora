import styled from "styled-components";

export const KeyboardOperations = styled.div`
  width: 100%;
  height: 100%;
  display: grid;
  grid-template-columns: 1fr;
  grid-template-rows: repeat(5, 1fr);
  grid-gap: 1px;
  box-sizing: border-box;
`;
